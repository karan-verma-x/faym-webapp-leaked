// Stub generated for the rebuild — original type definitions were
// tree-shaken out before the source map was generated, so we expose
// permissive `any` aliases so dependent components compile.

export type Section = any;
